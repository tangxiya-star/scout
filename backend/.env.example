# Copy to .env and fill in. Without ANTHROPIC_API_KEY the reasoning engine
# runs in deterministic fallback mode (demo still works fully).
ANTHROPIC_API_KEY=
# Optional model override (default: claude-sonnet-5)
SCOUT_MODEL=claude-sonnet-5
